<%= form_for @task do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label "new task:" %> 
    <%= f.text_field :name %>

    <%= f.label "due:" %>
    <%= f.date_picker_text_field :due, :size => 9 %>
    <%= submit_tag "create task", :class=>"button" %>
  </p>
<% end %>